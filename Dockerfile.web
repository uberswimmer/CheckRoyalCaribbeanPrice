FROM nginx:stable-alpine
COPY web/nginx.conf /etc/nginx/nginx.conf
# Static files only. No checker, account configuration, or Docker socket.
USER nginx
EXPOSE 8080
ENTRYPOINT ["nginx", "-g", "daemon off;"]
